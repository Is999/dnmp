server {
    listen       80;
    server_name  admin.yhz.cc;
    root   /www/yhz/_example/highadmin;
    index  index.php index.html index.htm;
    #charset koi8-r;

    access_log  /var/log/nginx/nginx.yhz-admin.access.log;
    error_log  /var/log/nginx/nginx.yhz-admin.error.log;

    real_ip_header  X-Forwarded-For;
    real_ip_recursive on;
    gzip_static on;

    #error_page  404              /404.html;

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }

    # proxy the PHP scripts to Apache listening on 127.0.0.1:80
    #
    #location ~ \.php$ {
    #    proxy_pass   http://127.0.0.1;
    #}

    # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
    #
    location ~ [^/]\.php(/|$) {
        fastcgi_pass   php56:9000;
        include        fastcgi-php.conf;
        include        fastcgi_params;
    }

    # deny access to .htaccess files, if Apache's document root
    # concurs with nginx's one
    #
    #location ~ /\.ht {
    #    deny  all;
    #}
}

server {
    listen       80;
    server_name  web.yhz.cc;
    root   /www/yhz/_example/highgame;
    index  index.php index.html index.htm;
    #charset koi8-r;

    access_log  /var/log/nginx/nginx.yhz-web.access.log;
    error_log  /var/log/nginx/nginx.yhz-web.error.log;

    real_ip_header  X-Forwarded-For;
    real_ip_recursive on;
    gzip_static on;

    #error_page  404              /404.html;

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }

    # proxy the PHP scripts to Apache listening on 127.0.0.1:80
    #
    #location ~ \.php$ {
    #    proxy_pass   http://127.0.0.1;
    #}

    # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
    #
    location ~ [^/]\.php(/|$) {
        fastcgi_pass   php56:9000;
        include        fastcgi-php.conf;
        include        fastcgi_params;
    }

#     location ~ /activity_system {
#         rewrite ^/activity_system/(.*)$  /$1 break;
#         #proxy_redirect off;
#         #proxy_set_header Host activity.index;
#         #proxy_set_header X-Real-IP $remote_addr;
#         #proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         #proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_pass http://activity.index;
#     }
    # 去掉 /activity_system 前缀并反向代理
    location /activity_system/ {
        rewrite ^/activity_system/(.*)$ /$1 break;
        proxy_pass http://activity.index/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # deny access to .htaccess files, if Apache's document root
    # concurs with nginx's one
    #
    #location ~ /\.ht {
    #    deny  all;
    #}
}

server {
    listen       80;
    server_name  h5.yhz.cc;
    root   /www/yhz/_example/mobilegame;
    index  index.php index.html index.htm;
    #charset koi8-r;

    access_log  /var/log/nginx/nginx.yhz-h5.access.log;
    error_log  /var/log/nginx/nginx.yhz-h5.error.log;

    real_ip_header  X-Forwarded-For;
    real_ip_recursive on;
    gzip_static on;

    #error_page  404              /404.html;

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }

    # proxy the PHP scripts to Apache listening on 127.0.0.1:80
    #
    #location ~ \.php$ {
    #    proxy_pass   http://127.0.0.1;
    #}

    # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
    #
    location ~ [^/]\.php(/|$) {
        fastcgi_pass   php56:9000;
        include        fastcgi-php.conf;
        include        fastcgi_params;
    }

    location /activity_system/ {
        rewrite ^/activity_system/(.*)$ /$1 break;
        proxy_pass http://activity.index/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

   #location ^~/images/ {
   #    alias /www/yhz/resources/images/;
   #    location ~ \.(js|css|gif|jpg|jpeg|png|bmp|swf)$  {
   #        expires 1d;
   #    }
   #}

   #location ^~/js/ {
   #    alias /www/yhz/resources/js/;
   #    location ~ \.(js|css|gif|jpg|jpeg|png|bmp|swf)$  {
   #        expires 1d;
   #    }
   #}

   #location ^~/css/ {
   #    alias /www/yhz/resources/css/;
   #    location ~ \.(js|css|gif|jpg|jpeg|png|bmp|swf)$  {
   #        expires 1d;
   #    }
   #}

    # deny access to .htaccess files, if Apache's document root
    # concurs with nginx's one
    #
    #location ~ /\.ht {
    #    deny  all;
    #}
}

server {
    listen       80;
    server_name  api.yhz.cc;
    root   /www/yhz/_example/portable;
    index  index.php index.html index.htm;
    #charset koi8-r;

    access_log  /var/log/nginx/nginx.yhz-api.access.log;
    error_log  /var/log/nginx/nginx.yhz-api.error.log;

    real_ip_header  X-Forwarded-For;
    real_ip_recursive on;

    #error_page  404              /404.html;

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }

    # proxy the PHP scripts to Apache listening on 127.0.0.1:80
    #
    #location ~ \.php$ {
    #    proxy_pass   http://127.0.0.1;
    #}

    # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
    #
    location ~ [^/]\.php(/|$) {
        fastcgi_pass   php56:9000;
        include        fastcgi-php.conf;
        include        fastcgi_params;
    }

    # deny access to .htaccess files, if Apache's document root
    # concurs with nginx's one
    #
    #location ~ /\.ht {
    #    deny  all;
    #}
}